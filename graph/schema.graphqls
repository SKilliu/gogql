# GraphQL schema
#

type User {
  # The ID of the character
  id: ID!
  name: String!
  email: String!
  createdAt: String
}

input NewUser {
  name: String!
  email: String!
}

type Query {
  users(id: String): [User!]!
}

type Mutation {
  createUser(input: NewUser!): User!
}
